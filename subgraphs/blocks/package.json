{
  "name": "blocks",
  "description": "PandaSwap Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:pandaswap/panda-subgraph.git",
  "author": "PandaSwap",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ pandaswap/blocks subgraph.yaml"
  }
}
